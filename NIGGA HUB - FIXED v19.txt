-- ================================================================================
-- CURVED DASH-BEHIND MOBILE v2 — JOYSTICK FIX
-- Root cause confirmed via MCP:
--   VIM:SendKeyEvent fires UIS keyboard events → mobile touch state resets → joystick dies
--   Fix: tc:UseKey(keyNum, "Activated/Deactivated") = native game mobile skill path
--   Verified: tc:UseKey fires ZERO UIS keyboard events ✓
-- ================================================================================
local Players          = game:GetService("Players")
local Workspace        = game:GetService("Workspace")
local UserInputService = game:GetService("UserInputService")
local RunService       = game:GetService("RunService")
local TweenService     = game:GetService("TweenService")
local Stats            = game:GetService("Stats")
local Knit             = require(game.ReplicatedStorage.Knit.Knit)

local player = Players.LocalPlayer

local CFG = {
    CURVE_BOW       = 5.0,
    OUT_BOW         = 2.0,
    BACK_DISTANCE   = 4.5,
    AUTO_BLACKFLASH = true,
    ARRIVE_DIST     = 1.2,
    BF_MAX_WHIFF    = 4.0,
}

local DASH_RANGE        = 10
local DASH_COOLDOWN     = 0.4
local DASH_DURATION     = 0.28
local DASH_MAX_DURATION = 0.36
local PREDICT_LEAD      = 0.32
local MAX_SPEED         = 320
local VEL_SMOOTH_K      = 55
local FACE_K            = 26
local TRACK_K           = 42
local YAW_TRACK_K       = 35
local ACCEL_TRACK_K     = 14
local EARLY_MIN_FRAC    = 0.35
local BEHIND_FINISH_DEG = 62
local PHYSICS_YAW_BLEND = 0.75
local FACELOCK_LEAD_BOOST = 1.6

local NO_DASH_GAP   = 4.0
local BACK_SIDE_DOT = -0.15

local YAW_ACCEL_TRACK_K  = 12
local SNAP_YAW_THRESHOLD = 4.0
local SNAP_LEAD_BOOST    = 2.2
local ADAPT_YAW_SCALE    = 3.0
local ADAPT_VEL_SCALE    = 22
local ADAPT_MAX_MULT     = 4.0
local POS_PREDICT_BOOST  = 1.15
local REACQUIRE_BEFORE_BF = true

local CHAIN_CORRECT_THRESHOLD = 1.6
local CHAIN_CORRECT_ENABLED   = true
local CHAIN_DASH_SETTLE       = 0.22

-- BF key number: matches skill slot (1-8)
-- KeyCode.Three = slot 3. Change to match whichever move you BF with.
local BF = {
    KEY_NUM  = 3,
    TIMING   = 0.26,
    HOLD     = 0.02,
    PING_FACTOR = 0.5,
    PING_MAX    = 0.06,
    INPUT_LAT   = 0.012,
    PING_SMOOTH = 0.40,
    FINE_SPIN   = 0.003,
}

-- Get ToolController once (same table the game uses for mobile buttons)
local tc = Knit.GetController("ToolController")

local lastDashTime   = 0
local isDashing      = false
local dashConnection = nil
local movementController = nil
local bfActive       = false

local trackedModel, lastTPos, lastTLook, lastSampleT
local estVel         = Vector3.zero
local estAccel       = Vector3.zero
local estYawRate     = 0
local estYawAccel    = 0
local estPhysYaw     = 0
local estBlendedYaw  = 0
local faceLockConf   = 0
local lastTLookRaw   = nil
local myLastPos      = nil

-- ── ping ─────────────────────────────────────────────────────────────────────
local function getPing()
    local ok, ping = pcall(function() return player:GetNetworkPing() end)
    if ok and type(ping) == "number" and ping > 0 then return ping end
    local ok2, ms = pcall(function()
        return Stats.Network.ServerStatsItem["Data Ping"]:GetValue()
    end)
    if ok2 and type(ms) == "number" and ms > 0 then return (ms/1000)*0.5 end
    return 0
end

local smoothedPing = nil
local function getSmoothedPing()
    local p = getPing()
    if not smoothedPing then smoothedPing = p
    else smoothedPing = smoothedPing + (p - smoothedPing) * BF.PING_SMOOTH end
    return smoothedPing
end

-- ── math helpers ─────────────────────────────────────────────────────────────
local function horizontal(v) return Vector3.new(v.X, 0, v.Z) end
local function damp(k, dt)   return 1 - math.exp(-k * dt) end
local function easeOutCirc(x)
    x = math.clamp(x, 0, 1)
    return math.sqrt(1 - (x-1)*(x-1))
end
local function rotateY(v, a)
    local c, s = math.cos(a), math.sin(a)
    return Vector3.new(c*v.X + s*v.Z, v.Y, -s*v.X + c*v.Z)
end
local function signedYawBetween(a, b)
    local dot = math.clamp(a:Dot(b), -1, 1)
    local ang = math.acos(dot)
    local cY  = a.X * b.Z - a.Z * b.X
    return (cY >= 0) and -ang or ang
end
local function adaptiveK(baseK, deviation, scale)
    local mult = 1 + math.clamp(deviation / scale, 0, ADAPT_MAX_MULT - 1)
    return baseK * mult
end

-- ── character helpers ─────────────────────────────────────────────────────────
local function getCharacter()
    local char = player.Character
    if char and char:FindFirstChild("HumanoidRootPart") and char:FindFirstChild("Humanoid") then
        return char
    end
end

local function getMovementController()
    if movementController and rawget(movementController, "Dash") then return movementController end
    movementController = nil
    for _, m in pairs(getreg()) do
        if typeof(m) == "table" and rawget(m, "Name") == "MovementController" and rawget(m, "Dash") then
            movementController = m; break
        end
    end
    return movementController
end

local function nukeGameBV(character)
    for _, d in ipairs(character:GetDescendants()) do
        if d:IsA("BodyVelocity") then
            pcall(function() d.MaxForce = Vector3.zero; d.Velocity = Vector3.zero; d:Destroy() end)
        end
    end
end

-- ── target finding ────────────────────────────────────────────────────────────
local function findTarget()
    local character = getCharacter()
    if not character then return nil, math.huge end
    local rootPart = character.HumanoidRootPart
    local closest, closestDist = nil, DASH_RANGE + 1
    local folder = Workspace:FindFirstChild("Characters")
    if folder then
        for _, obj in ipairs(folder:GetChildren()) do
            if obj:IsA("Model") and obj ~= character then
                local hum  = obj:FindFirstChild("Humanoid")
                local part = obj.PrimaryPart or obj:FindFirstChild("HumanoidRootPart")
                if hum and part and hum.Health > 0 then
                    local dist = (part.Position - rootPart.Position).Magnitude
                    if dist <= DASH_RANGE and dist < closestDist then
                        closestDist, closest = dist, obj
                    end
                end
            end
        end
    end
    return closest, closestDist
end

-- ── live back geometry ────────────────────────────────────────────────────────
local function liveBackInfo(target)
    local char = getCharacter()
    local part = target and (target.PrimaryPart or target:FindFirstChild("HumanoidRootPart"))
    if not char or not part then return math.huge, 1 end
    local hrp  = char.HumanoidRootPart
    local look = horizontal(part.CFrame.LookVector)
    if look.Magnitude < 0.01 then look = lastTLook or Vector3.new(0,0,1) end
    look = look.Unit
    local backH = horizontal(part.Position) - look * CFG.BACK_DISTANCE
    local gap   = (horizontal(hrp.Position) - backH).Magnitude
    local toMe  = horizontal(hrp.Position - part.Position)
    local dot   = (toMe.Magnitude > 0.05) and look:Dot(toMe.Unit) or -1
    return gap, dot
end

local function alreadyAtBack(target)
    if not target then return false end
    local gap, dot = liveBackInfo(target)
    return gap <= NO_DASH_GAP and dot <= BACK_SIDE_DOT
end

local function isPosBehind(target, myPos)
    local part = target and (target.PrimaryPart or target:FindFirstChild("HumanoidRootPart"))
    if not part then return false end
    local look = horizontal(part.CFrame.LookVector)
    if look.Magnitude < 0.01 then look = lastTLook end
    if not look or look.Magnitude < 0.01 then return false end
    look = look.Unit
    local toMe = horizontal(myPos - part.Position)
    if toMe.Magnitude < 0.1 then return true end
    return look:Dot(toMe.Unit) <= -math.cos(math.rad(BEHIND_FINISH_DEG))
end

-- ── tracker ───────────────────────────────────────────────────────────────────
local function updateTracker(target)
    local part = target and (target.PrimaryPart or target:FindFirstChild("HumanoidRootPart"))
    if not part then trackedModel = nil; return end
    local now  = tick()
    local pos  = part.Position
    local rawLook = horizontal(part.CFrame.LookVector)
    local look = (rawLook.Magnitude > 0.01) and rawLook.Unit or lastTLook
    local physYawRaw = -part.AssemblyAngularVelocity.Y
    local char = getCharacter()
    local myPos = char and char:FindFirstChild("HumanoidRootPart") and char.HumanoidRootPart.Position

    if trackedModel == target and lastSampleT and look then
        local dt = now - lastSampleT
        if dt > 1e-4 then
            local instVel = (pos - lastTPos) / dt
            local velDev  = (instVel - estVel).Magnitude
            local kVel    = adaptiveK(TRACK_K, velDev, ADAPT_VEL_SCALE)
            local newEstVel = estVel:Lerp(instVel, damp(kVel, dt))
            local instAcc = (newEstVel - estVel) / dt
            estAccel = estAccel:Lerp(instAcc, damp(ACCEL_TRACK_K, dt))
            estVel   = newEstVel

            if lastTLookRaw then
                local instDerivedYaw = signedYawBetween(lastTLookRaw, look) / dt
                local yawDev = math.abs(instDerivedYaw - estYawRate)
                local kYaw   = adaptiveK(YAW_TRACK_K, yawDev, ADAPT_YAW_SCALE)
                local prevYaw = estYawRate
                estYawRate = estYawRate + (instDerivedYaw - estYawRate) * damp(kYaw, dt)
                local instYawAccel = (estYawRate - prevYaw) / dt
                estYawAccel = estYawAccel + (instYawAccel - estYawAccel) * damp(YAW_ACCEL_TRACK_K, dt)
            end

            physYawRaw = math.clamp(physYawRaw, -15, 15)
            estPhysYaw = estPhysYaw + (physYawRaw - estPhysYaw) * damp(50, dt)
            estBlendedYaw = PHYSICS_YAW_BLEND * estPhysYaw + (1 - PHYSICS_YAW_BLEND) * estYawRate

            if myPos and myLastPos then
                local toMeNow  = horizontal(myPos - pos)
                local toMePrev = horizontal(myLastPos - pos)
                if toMeNow.Magnitude > 0.1 and toMePrev.Magnitude > 0.1 then
                    local totalBearingDelta = signedYawBetween(toMePrev.Unit, toMeNow.Unit) / dt
                    local myMoveDelta = horizontal(myPos - myLastPos)
                    local perpContrib = 0
                    if toMeNow.Magnitude > 0.1 then
                        local radialDir = toMeNow.Unit
                        local perpDir   = Vector3.new(-radialDir.Z, 0, radialDir.X)
                        perpContrib = myMoveDelta:Dot(perpDir) / math.max(toMeNow.Magnitude, 1)
                    end
                    local theirRotTowardUs = totalBearingDelta - perpContrib
                    local toMeDir    = toMeNow.Unit
                    local facingDot  = look:Dot(toMeDir)
                    local rotSignal  = math.clamp(theirRotTowardUs / 3.0, 0, 1)
                    local facingSignal = math.clamp((facingDot + 1) / 2, 0, 1)
                    local rawConf    = rotSignal * 0.6 + facingSignal * 0.4
                    local decayK     = (facingDot < -0.5) and 8 or 3
                    local buildK     = (theirRotTowardUs > 0.3) and 6 or 1.5
                    local k = (rawConf > faceLockConf) and buildK or decayK
                    faceLockConf = faceLockConf + (rawConf - faceLockConf) * damp(k, dt)
                    faceLockConf = math.clamp(faceLockConf, 0, 1)
                end
            end
        end
    else
        estVel, estAccel = Vector3.zero, Vector3.zero
        estYawRate    = 0; estYawAccel   = 0
        estPhysYaw    = physYawRaw; estBlendedYaw = physYawRaw
        faceLockConf  = 0
    end

    trackedModel = target; lastTPos = pos; lastSampleT = now
    lastTLookRaw = look; lastTLook = look; myLastPos = myPos
end

local function predictBack(target, lead)
    local part = target.PrimaryPart or target:FindFirstChild("HumanoidRootPart")
    local pos  = part.Position
    local accLead = estAccel * (0.5 * lead * lead)
    if accLead.Magnitude > 6 then accLead = accLead.Unit * 6 end
    local centerH = horizontal(pos + estVel * lead * POS_PREDICT_BOOST + accLead)
    local baseLook = horizontal(part.CFrame.LookVector)
    baseLook = (baseLook.Magnitude > 0.01) and baseLook.Unit or (lastTLook or Vector3.new(0,0,1))

    local lock = math.clamp(faceLockConf, 0, 1)
    local effectiveLead = math.clamp(lead * (1 + lock * (FACELOCK_LEAD_BOOST - 1)), lead, lead * FACELOCK_LEAD_BOOST)
    local snapMul = (math.abs(estBlendedYaw) > SNAP_YAW_THRESHOLD) and SNAP_LEAD_BOOST or 1
    local effYaw = math.clamp(estBlendedYaw, -14, 14)
    local yawPredict = math.clamp(
        (effYaw * effectiveLead + 0.5 * math.clamp(estYawAccel,-120,120) * effectiveLead^2) * snapMul,
        -math.pi, math.pi)
    local predLook = rotateY(baseLook, yawPredict).Unit
    return centerH, centerH - predLook * CFG.BACK_DISTANCE, predLook
end

-- ── BLACK FLASH — uses tc:UseKey instead of VIM ──────────────
-- tc:UseKey(keyNum, "Activated")   = press  (fires zero UIS keyboard events ✓)
-- tc:UseKey(keyNum, "Deactivated") = release
local lastBFComp = 0
local function bfComputeEff()
    local ping = getSmoothedPing()
    local comp = math.clamp(ping * BF.PING_FACTOR, 0, BF.PING_MAX)
    lastBFComp = comp
    return math.max(0.05, BF.TIMING + comp - BF.INPUT_LAT)
end

local function faceTargetOnce(target)
    local char = getCharacter()
    local part = target and (target.PrimaryPart or target:FindFirstChild("HumanoidRootPart"))
    if not char or not part then return end
    local hrp = char.HumanoidRootPart
    local fd = horizontal(part.Position - hrp.Position)
    if fd.Magnitude > 0.05 then
        hrp.CFrame = CFrame.lookAt(hrp.Position,
            Vector3.new(part.Position.X, hrp.Position.Y, part.Position.Z))
    end
end

local function bfFireCharged(effTime, target, skipOrient)
    if bfActive then return end
    bfActive = true
    task.spawn(function()
        if (not skipOrient) and REACQUIRE_BEFORE_BF and target then
            faceTargetOnce(target)
        end

        -- HIT 1: press + release via native game path, NO VIM
        pcall(function() tc:UseKey(BF.KEY_NUM, "Activated") end)
        task.wait(BF.HOLD)
        pcall(function() tc:UseKey(BF.KEY_NUM, "Deactivated") end)

        -- Wait the golden interval
        local target_t = os.clock() + (effTime - BF.HOLD)
        while bfActive and (target_t - os.clock()) > BF.FINE_SPIN do
            RunService.Heartbeat:Wait()
        end
        while bfActive and os.clock() < target_t do end

        -- HIT 2: fires the Black Flash
        pcall(function() tc:UseKey(BF.KEY_NUM, "Activated") end)
        task.wait(BF.HOLD)
        pcall(function() tc:UseKey(BF.KEY_NUM, "Deactivated") end)

        bfActive = false
    end)
end

-- ── chain correction dash ─────────────────────────────────────────────────────
local function chainCorrectionDash(target)
    if not CHAIN_CORRECT_ENABLED then return false end
    local controller = getMovementController()
    local char = getCharacter()
    if not controller or not char then return false end
    local hrp  = char.HumanoidRootPart
    local part = target.PrimaryPart or target:FindFirstChild("HumanoidRootPart")
    if not hrp or not part then return false end

    local _, backPos = predictBack(target, math.min(CHAIN_DASH_SETTLE, PREDICT_LEAD))
    backPos = horizontal(backPos)
    if (horizontal(hrp.Position) - backPos).Magnitude <= CHAIN_CORRECT_THRESHOLD then return false end

    local humanoid = char:FindFirstChild("Humanoid")
    if humanoid then humanoid.AutoRotate = false end
    local faceCF = CFrame.lookAt(hrp.Position,
        Vector3.new(part.Position.X, hrp.Position.Y, part.Position.Z))
    hrp.CFrame = faceCF

    local toBack = backPos - horizontal(hrp.Position)
    if toBack.Magnitude < 0.01 then
        if humanoid then humanoid.AutoRotate = true end; return false
    end
    local dashDir = (horizontal(faceCF.RightVector).Unit:Dot(toBack.Unit) >= 0) and "Right" or "Left"

    if not pcall(function() controller:Dash(player, char, dashDir) end) then
        if humanoid then humanoid.AutoRotate = true end; return false
    end

    local startTime = tick(); local smVelH = Vector3.zero; local corrConn
    corrConn = RunService.Stepped:Connect(function(_, dt)
        dt = math.clamp(dt, 1/240, 1/30)
        local c = getCharacter(); if not c then corrConn:Disconnect(); return end
        local h = c.HumanoidRootPart; local elapsed = tick() - startTime
        nukeGameBV(c)
        local _, tb = predictBack(target, math.clamp(CHAIN_DASH_SETTLE - elapsed, 0.05, PREDICT_LEAD))
        local toBack2 = horizontal(tb) - horizontal(h.Position)
        if toBack2.Magnitude <= 0.8 or elapsed >= CHAIN_DASH_SETTLE then
            h.AssemblyLinearVelocity = Vector3.new(0, h.AssemblyLinearVelocity.Y, 0)
            local fd = horizontal(part.Position - h.Position)
            if fd.Magnitude > 0.05 then
                h.CFrame = CFrame.lookAt(h.Position, Vector3.new(part.Position.X, h.Position.Y, part.Position.Z))
            end
            if humanoid then humanoid.AutoRotate = true end
            corrConn:Disconnect(); return
        end
        local p = easeOutCirc(elapsed / CHAIN_DASH_SETTLE)
        local velH = (toBack2 / dt) * math.min(1, p + 0.4)
        smVelH = smVelH:Lerp(velH, damp(VEL_SMOOTH_K, dt))
        if smVelH.Magnitude > MAX_SPEED then smVelH = smVelH.Unit * MAX_SPEED end
        h.AssemblyLinearVelocity = Vector3.new(smVelH.X, h.AssemblyLinearVelocity.Y, smVelH.Z)
        local goalLook = horizontal(part.Position - h.Position)
        if goalLook.Magnitude > 0.05 then
            local goalRot = CFrame.lookAt(h.Position, h.Position + goalLook.Unit)
            local curRot  = h.CFrame - h.CFrame.Position
            h.CFrame = CFrame.new(h.Position) * curRot:Lerp(goalRot - goalRot.Position, damp(FACE_K, dt))
        end
    end)
    task.wait(CHAIN_DASH_SETTLE + 0.02)
    return true
end

local function startBlackFlash(target)
    if bfActive then return end
    if target and CHAIN_CORRECT_ENABLED and not alreadyAtBack(target) then
        local _, predBack = predictBack(target, PREDICT_LEAD)
        local char = getCharacter()
        if char then
            local gap = (horizontal(char.HumanoidRootPart.Position) - horizontal(predBack)).Magnitude
            if gap > CHAIN_CORRECT_THRESHOLD then chainCorrectionDash(target) end
        end
    end
    bfFireCharged(bfComputeEff(), target, false)
end

-- ── main curved dash ──────────────────────────────────────────────────────────
local function executeCurvedDash(target)
    local controller = getMovementController()
    if not controller then return false end
    local character  = getCharacter()
    if not character or not target then return false end
    local hrp        = character.HumanoidRootPart
    local humanoid   = character:FindFirstChild("Humanoid")
    local targetPart = target.PrimaryPart or target:FindFirstChild("HumanoidRootPart")
    if not targetPart then return false end

    local liveGap, liveDot = liveBackInfo(target)
    if liveGap <= NO_DASH_GAP and liveDot <= BACK_SIDE_DOT then
        if CFG.AUTO_BLACKFLASH then startBlackFlash(target) end
        return true
    end

    local center0, back0 = predictBack(target, math.min(DASH_DURATION, PREDICT_LEAD))
    local radial = horizontal(hrp.Position - center0)
    if radial.Magnitude < 0.1 then radial = -horizontal(hrp.CFrame.LookVector) end
    radial = radial.Unit
    local tangentCCW = Vector3.new(-radial.Z, 0, radial.X)
    local orbitSign  = (tangentCCW:Dot(horizontal(back0 - hrp.Position)) >= 0) and 1 or -1
    local faceCF     = CFrame.lookAt(hrp.Position, Vector3.new(center0.X, hrp.Position.Y, center0.Z))
    local dashDir    = ((tangentCCW * orbitSign):Dot(faceCF.RightVector) >= 0) and "Right" or "Left"
    hrp.CFrame       = faceCF

    local startPosH = horizontal(hrp.Position)
    local sideBow   = horizontal((dashDir == "Right") and faceCF.RightVector or -faceCF.RightVector)
    if sideBow.Magnitude < 0.01 then sideBow = horizontal(tangentCCW * orbitSign) end
    sideBow = sideBow.Unit
    local outDir = (startPosH - horizontal(center0))
    outDir = (outDir.Magnitude > 0.1) and outDir.Unit or -horizontal(faceCF.LookVector).Unit

    if not pcall(function() controller:Dash(player, character, dashDir) end) then return false end
    isDashing = true
    if humanoid then humanoid.AutoRotate = false end

    local dashEffTime = bfComputeEff()
    local bfArmed = false; local bvKilled = false
    local startTime = tick(); local smVelH = Vector3.zero
    if dashConnection then dashConnection:Disconnect() end

    local function finish(hrp2)
        nukeGameBV(character)
        hrp2.AssemblyLinearVelocity = Vector3.new(0, hrp2.AssemblyLinearVelocity.Y, 0)
        local fd = horizontal(targetPart.Position - hrp2.Position)
        if fd.Magnitude > 0.05 then
            hrp2.CFrame = CFrame.lookAt(hrp2.Position,
                Vector3.new(targetPart.Position.X, hrp2.Position.Y, targetPart.Position.Z))
        end
        if humanoid then humanoid.AutoRotate = true end
        isDashing = false
        if dashConnection then dashConnection:Disconnect(); dashConnection = nil end
        local _, finalBack = predictBack(target, 0)
        local landGap = horizontal(hrp2.Position - finalBack).Magnitude
        print(string.format("[Dash] landed %.2fst | behind:%s | ping+%.0fms",
            landGap, tostring(isPosBehind(target, hrp2.Position)), lastBFComp*1000))
        if CFG.AUTO_BLACKFLASH and not bfArmed and not bfActive and landGap <= CFG.BF_MAX_WHIFF then
            bfFireCharged(dashEffTime, target, false)
        end
    end

    dashConnection = RunService.Stepped:Connect(function(_, dt)
        dt = math.clamp(dt, 1/240, 1/30)
        local char = getCharacter(); if not char or not isDashing then return end
        local hrp2 = char.HumanoidRootPart
        local elapsed = tick() - startTime; local pos = hrp2.Position
        if not bvKilled then nukeGameBV(char); bvKilled = true end

        local remaining = math.max(0, DASH_DURATION - elapsed)
        local lead = math.clamp(remaining, 0.05, PREDICT_LEAD)
        local _, backH = predictBack(target, lead)
        local dist = (horizontal(pos) - backH).Magnitude

        if CFG.AUTO_BLACKFLASH and not bfArmed and not bfActive and remaining <= dashEffTime then
            bfArmed = true
            bfFireCharged(dashEffTime, target, true)
        end

        if elapsed >= DASH_DURATION * EARLY_MIN_FRAC and dist <= CFG.ARRIVE_DIST and isPosBehind(target, pos) then
            finish(hrp2); return
        end
        if elapsed >= DASH_MAX_DURATION then finish(hrp2); return end

        local desiredPos
        if elapsed < DASH_DURATION then
            local p = easeOutCirc(elapsed / DASH_DURATION)
            local chord = backH - startPosH
            local mid = startPosH + chord * 0.5
            local control = mid + sideBow*(chord.Magnitude*0.55 + CFG.CURVE_BOW) + outDir*(CFG.OUT_BOW*(1-p))
            local omp = 1 - p
            desiredPos = startPosH*(omp*omp) + control*(2*omp*p) + backH*(p*p)
        else
            local d = backH - horizontal(pos)
            desiredPos = (d.Magnitude > 0.05) and (horizontal(pos) + d.Unit*(MAX_SPEED*dt)) or backH
        end

        local velH = (desiredPos - horizontal(pos)) / dt
        smVelH = smVelH:Lerp(velH, damp(VEL_SMOOTH_K, dt))
        if smVelH.Magnitude > MAX_SPEED then smVelH = smVelH.Unit * MAX_SPEED end
        hrp2.AssemblyLinearVelocity = Vector3.new(smVelH.X, hrp2.AssemblyLinearVelocity.Y, smVelH.Z)

        local goalLook = horizontal(targetPart.Position - pos)
        if goalLook.Magnitude > 0.05 then
            local goalRot = CFrame.lookAt(pos, pos + goalLook.Unit)
            local curRot  = hrp2.CFrame - hrp2.CFrame.Position
            hrp2.CFrame = CFrame.new(pos) * curRot:Lerp(goalRot - goalRot.Position, damp(FACE_K, dt))
        end
    end)
    return true
end

-- ================================================================================
-- MOBILE GUI
-- ================================================================================
local oldGui = player.PlayerGui:FindFirstChild("DashBehindMobile")
if oldGui then oldGui:Destroy() end

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "DashBehindMobile"
screenGui.ResetOnSpawn = false
screenGui.DisplayOrder = 200
screenGui.IgnoreGuiInset = true
screenGui.Parent = player.PlayerGui

local statusLabel = Instance.new("TextLabel")
statusLabel.Size = UDim2.new(0, 280, 0, 28)
statusLabel.Position = UDim2.new(0.5, -140, 0, 60)
statusLabel.BackgroundColor3 = Color3.fromRGB(10,10,20)
statusLabel.BackgroundTransparency = 0.3
statusLabel.TextColor3 = Color3.fromRGB(200,200,200)
statusLabel.Font = Enum.Font.GothamBold; statusLabel.TextSize = 13
statusLabel.Text = "Ready"; statusLabel.BorderSizePixel = 0
statusLabel.Parent = screenGui
Instance.new("UICorner", statusLabel).CornerRadius = UDim.new(0,8)

-- Settings panel
local panel = Instance.new("Frame")
panel.Size = UDim2.new(0,260,0,390); panel.Position = UDim2.new(0,10,0,100)
panel.BackgroundColor3 = Color3.fromRGB(18,18,26); panel.BackgroundTransparency = 0.08
panel.BorderSizePixel = 0; panel.Active = true; panel.Parent = screenGui
Instance.new("UICorner", panel).CornerRadius = UDim.new(0,14)
local pStroke = Instance.new("UIStroke", panel)
pStroke.Color = Color3.fromRGB(0,170,255); pStroke.Thickness = 1.5; pStroke.Transparency = 0.4

local titleBar = Instance.new("Frame")
titleBar.Size = UDim2.new(1,0,0,38); titleBar.BackgroundColor3 = Color3.fromRGB(0,130,210)
titleBar.BackgroundTransparency = 0.15; titleBar.BorderSizePixel = 0
titleBar.Active = true; titleBar.Parent = panel
Instance.new("UICorner", titleBar).CornerRadius = UDim.new(0,14)

local titleTxt = Instance.new("TextLabel", titleBar)
titleTxt.Size = UDim2.new(1,-70,1,0); titleTxt.Position = UDim2.new(0,12,0,0)
titleTxt.BackgroundTransparency = 1; titleTxt.TextXAlignment = Enum.TextXAlignment.Left
titleTxt.Text = "⚔ Dash Behind v2 Mobile"; titleTxt.TextColor3 = Color3.fromRGB(255,255,255)
titleTxt.Font = Enum.Font.GothamBold; titleTxt.TextSize = 13

local collapseBtn = Instance.new("TextButton", titleBar)
collapseBtn.Size = UDim2.new(0,56,0,26); collapseBtn.Position = UDim2.new(1,-62,0.5,-13)
collapseBtn.BackgroundColor3 = Color3.fromRGB(255,255,255); collapseBtn.BackgroundTransparency = 0.82
collapseBtn.Text = "Hide"; collapseBtn.TextColor3 = Color3.fromRGB(255,255,255)
collapseBtn.Font = Enum.Font.GothamBold; collapseBtn.TextSize = 12; collapseBtn.BorderSizePixel = 0
Instance.new("UICorner", collapseBtn).CornerRadius = UDim.new(0,7)

local panelContent = Instance.new("Frame", panel)
panelContent.Size = UDim2.new(1,0,1,-38); panelContent.Position = UDim2.new(0,0,0,38)
panelContent.BackgroundTransparency = 1

local collapsed = false
collapseBtn.MouseButton1Click:Connect(function()
    collapsed = not collapsed; panelContent.Visible = not collapsed
    panel.Size = collapsed and UDim2.new(0,260,0,38) or UDim2.new(0,260,0,390)
    collapseBtn.Text = collapsed and "Show" or "Hide"
end)

-- Drag
do
    local dragging, dragStart, startPos = false, nil, nil
    titleBar.InputBegan:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1
        or i.UserInputType == Enum.UserInputType.Touch then
            dragging=true; dragStart=i.Position; startPos=panel.Position
        end
    end)
    UserInputService.InputChanged:Connect(function(i)
        if dragging and (i.UserInputType == Enum.UserInputType.MouseMovement
        or i.UserInputType == Enum.UserInputType.Touch) then
            local d = i.Position - dragStart
            panel.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset+d.X,
                startPos.Y.Scale, startPos.Y.Offset+d.Y)
        end
    end)
    UserInputService.InputEnded:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1
        or i.UserInputType == Enum.UserInputType.Touch then dragging=false end
    end)
end

local function makeSlider(parent, yPos, name, minV, maxV, default, decimals, onChange)
    local holder = Instance.new("Frame", parent)
    holder.Size = UDim2.new(1,-24,0,46); holder.Position = UDim2.new(0,12,0,yPos)
    holder.BackgroundTransparency = 1
    local label = Instance.new("TextLabel", holder)
    label.Size = UDim2.new(1,0,0,18); label.BackgroundTransparency = 1
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Font = Enum.Font.Gotham; label.TextSize = 13
    label.TextColor3 = Color3.fromRGB(220,220,220)
    local function fmt(v) return string.format("%."..decimals.."f", v) end
    local bar = Instance.new("Frame", holder)
    bar.Size = UDim2.new(1,0,0,8); bar.Position = UDim2.new(0,0,0,28)
    bar.BackgroundColor3 = Color3.fromRGB(60,60,70); bar.BorderSizePixel = 0; bar.Active = true
    Instance.new("UICorner", bar).CornerRadius = UDim.new(1,0)
    local fill = Instance.new("Frame", bar)
    fill.BackgroundColor3 = Color3.fromRGB(0,185,255); fill.BorderSizePixel = 0
    Instance.new("UICorner", fill).CornerRadius = UDim.new(1,0)
    local knob = Instance.new("Frame", bar)
    knob.Size = UDim2.new(0,20,0,20); knob.AnchorPoint = Vector2.new(0.5,0.5)
    knob.BackgroundColor3 = Color3.fromRGB(255,255,255); knob.BorderSizePixel = 0; knob.Active = true
    Instance.new("UICorner", knob).CornerRadius = UDim.new(1,0)
    local function apply(a)
        a = math.clamp(a,0,1)
        fill.Size = UDim2.new(a,0,1,0); knob.Position = UDim2.new(a,0,0.5,0)
        label.Text = name..":  "..fmt(minV+(maxV-minV)*a)
        if onChange then onChange(minV+(maxV-minV)*a) end
    end
    apply((default-minV)/(maxV-minV))
    local dragging = false
    local function setX(px) apply((px-bar.AbsolutePosition.X)/math.max(1,bar.AbsoluteSize.X)) end
    local function dn(i)
        if i.UserInputType==Enum.UserInputType.MouseButton1 or i.UserInputType==Enum.UserInputType.Touch then
            dragging=true; setX(i.Position.X) end end
    bar.InputBegan:Connect(dn); knob.InputBegan:Connect(dn)
    UserInputService.InputChanged:Connect(function(i)
        if dragging and (i.UserInputType==Enum.UserInputType.MouseMovement or i.UserInputType==Enum.UserInputType.Touch) then setX(i.Position.X) end end)
    UserInputService.InputEnded:Connect(function(i)
        if i.UserInputType==Enum.UserInputType.MouseButton1 or i.UserInputType==Enum.UserInputType.Touch then dragging=false end end)
end

local function makeToggle(parent, yPos, labelOn, labelOff, default, color, onChange)
    local btn = Instance.new("TextButton", parent)
    btn.Size = UDim2.new(1,-24,0,36); btn.Position = UDim2.new(0,12,0,yPos)
    btn.BorderSizePixel = 0; btn.Font = Enum.Font.GothamBold; btn.TextSize = 13
    btn.TextColor3 = Color3.fromRGB(255,255,255)
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0,10)
    local state = default
    local function refresh()
        btn.Text = state and labelOn or labelOff
        btn.BackgroundColor3 = state and color or Color3.fromRGB(100,50,50)
    end
    refresh()
    btn.MouseButton1Click:Connect(function() state=not state; refresh(); if onChange then onChange(state) end end)
end

-- BF key slot selector (shows current slot number)
local bfSlotRow = Instance.new("Frame", panelContent)
bfSlotRow.Size = UDim2.new(1,-24,0,36); bfSlotRow.Position = UDim2.new(0,12,0,6)
bfSlotRow.BackgroundTransparency = 1
local bfSlotLabel = Instance.new("TextLabel", bfSlotRow)
bfSlotLabel.Size = UDim2.new(0.6,0,1,0); bfSlotLabel.BackgroundTransparency = 1
bfSlotLabel.TextXAlignment = Enum.TextXAlignment.Left
bfSlotLabel.TextColor3 = Color3.fromRGB(220,220,220)
bfSlotLabel.Font = Enum.Font.GothamBold; bfSlotLabel.TextSize = 13
bfSlotLabel.Text = "BF Skill Slot:  " .. BF.KEY_NUM
local function makeMiniBtn(parent, x, txt, clr, cb)
    local b = Instance.new("TextButton", parent)
    b.Size = UDim2.new(0,32,0,28); b.Position = UDim2.new(0,x,0.5,-14)
    b.BackgroundColor3 = clr; b.BorderSizePixel = 0
    b.Text = txt; b.TextColor3 = Color3.fromRGB(255,255,255)
    b.Font = Enum.Font.GothamBold; b.TextSize = 14
    Instance.new("UICorner", b).CornerRadius = UDim.new(0,6)
    b.MouseButton1Click:Connect(cb); return b
end
makeMiniBtn(bfSlotRow, 138, "−", Color3.fromRGB(160,60,60), function()
    BF.KEY_NUM = math.max(1, BF.KEY_NUM - 1)
    bfSlotLabel.Text = "BF Skill Slot:  " .. BF.KEY_NUM
end)
makeMiniBtn(bfSlotRow, 174, "+", Color3.fromRGB(60,140,60), function()
    BF.KEY_NUM = math.min(8, BF.KEY_NUM + 1)
    bfSlotLabel.Text = "BF Skill Slot:  " .. BF.KEY_NUM
end)

makeSlider(panelContent, 50,  "Curve Width",   0,   12,  CFG.CURVE_BOW,    1, function(v) CFG.CURVE_BOW    = v end)
makeSlider(panelContent, 96,  "Back Gap",      1,   8,   CFG.BACK_DISTANCE, 1, function(v) CFG.BACK_DISTANCE= v end)
makeSlider(panelContent, 142, "Predict Lead",  0.10,0.60,PREDICT_LEAD,     2, function(v) PREDICT_LEAD      = v end)
makeSlider(panelContent, 188, "No-Dash Range", 1.0, 8.0, NO_DASH_GAP,      1, function(v) NO_DASH_GAP       = v end)
makeSlider(panelContent, 234, "BF Timing",     0.15,0.40,BF.TIMING,        3, function(v) BF.TIMING         = v end)
makeSlider(panelContent, 280, "Dash Speed",    120, 600, MAX_SPEED,         0, function(v) MAX_SPEED         = v end)

makeToggle(panelContent, 330, "Auto Black Flash: ON","Auto Black Flash: OFF",
    true, Color3.fromRGB(150,40,160), function(v) CFG.AUTO_BLACKFLASH = v end)
makeToggle(panelContent, 370, "Chain Reposition: ON","Chain Reposition: OFF",
    true, Color3.fromRGB(170,110,0), function(v) CHAIN_CORRECT_ENABLED = v end)

-- Main action buttons
local dashBtn = Instance.new("TextButton")
dashBtn.Size = UDim2.new(0,110,0,110); dashBtn.Position = UDim2.new(1,-130,1,-200)
dashBtn.BackgroundColor3 = Color3.fromRGB(0,160,255); dashBtn.BackgroundTransparency = 0.15
dashBtn.BorderSizePixel = 0; dashBtn.Text = "DASH"
dashBtn.TextColor3 = Color3.fromRGB(255,255,255); dashBtn.Font = Enum.Font.GothamBold
dashBtn.TextSize = 22; dashBtn.Parent = screenGui
Instance.new("UICorner", dashBtn).CornerRadius = UDim.new(1,0)
local dStroke = Instance.new("UIStroke", dashBtn)
dStroke.Color = Color3.fromRGB(255,255,255); dStroke.Thickness = 2; dStroke.Transparency = 0.6

local flashBtn = Instance.new("TextButton")
flashBtn.Size = UDim2.new(0,110,0,110); flashBtn.Position = UDim2.new(0,20,1,-200)
flashBtn.BackgroundColor3 = Color3.fromRGB(160,30,200); flashBtn.BackgroundTransparency = 0.15
flashBtn.BorderSizePixel = 0; flashBtn.Text = "FLASH"
flashBtn.TextColor3 = Color3.fromRGB(255,255,255); flashBtn.Font = Enum.Font.GothamBold
flashBtn.TextSize = 22; flashBtn.Parent = screenGui
Instance.new("UICorner", flashBtn).CornerRadius = UDim.new(1,0)
local fStroke = Instance.new("UIStroke", flashBtn)
fStroke.Color = Color3.fromRGB(255,255,255); fStroke.Thickness = 2; fStroke.Transparency = 0.6

local function flashBtnPulse(btn, col)
    local orig = btn.BackgroundColor3
    btn.BackgroundColor3 = col
    TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3 = orig}):Play()
end

dashBtn.MouseButton1Click:Connect(function()
    if isDashing or bfActive then return end
    local target, dist = findTarget()
    if not target then statusLabel.Text = "No target within "..DASH_RANGE.." studs"; return end
    if alreadyAtBack(target) then
        statusLabel.Text = "On back — Flash!"
        flashBtnPulse(dashBtn, Color3.fromRGB(180,60,220))
        startBlackFlash(target); return
    end
    local now = tick()
    if now - lastDashTime < DASH_COOLDOWN then return end
    lastDashTime = now
    statusLabel.Text = string.format("Dash → %s (%.1f)", target.Name, dist)
    flashBtnPulse(dashBtn, Color3.fromRGB(255,255,100))
    executeCurvedDash(target)
end)

flashBtn.MouseButton1Click:Connect(function()
    flashBtnPulse(flashBtn, Color3.fromRGB(220,100,255))
    startBlackFlash(findTarget())
end)

RunService.Heartbeat:Connect(function()
    local target, dist = findTarget()
    updateTracker(target)
    if not isDashing then
        if target then
            local behind = alreadyAtBack(target)
            dashBtn.BackgroundColor3 = behind and Color3.fromRGB(160,40,200) or Color3.fromRGB(0,160,255)
            dashBtn.Text = behind and "FLASH" or "DASH"
            if not bfActive then
                statusLabel.Text = string.format("%s %s (%.1f) | lock:%.2f",
                    behind and "FLASH" or "DASH", target.Name, dist, faceLockConf)
            end
        else
            dashBtn.BackgroundColor3 = Color3.fromRGB(50,50,60)
            dashBtn.Text = "DASH"
            statusLabel.Text = "No target within "..DASH_RANGE.." studs"
        end
    end
    if bfActive then
        flashBtn.BackgroundColor3 = Color3.fromRGB(220,80,255)
        statusLabel.Text = "⚡ Black Flash..."
    else
        flashBtn.BackgroundColor3 = Color3.fromRGB(160,30,200)
    end
end)

print("DASH BEHIND MOBILE v2 — joystick fix — loaded")
